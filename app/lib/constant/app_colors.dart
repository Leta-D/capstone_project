import 'package:flutter/material.dart';

class AppColors {
  static Color green(double opacity) => Color.fromRGBO(18, 226, 129, opacity);
  static Color lightGreen(double opacity) =>
      Color.fromRGBO(112, 244, 187, opacity);
  static Color black(double opacity) => Color.fromRGBO(4, 11, 4, opacity);
  static Color white(double opacity) => Color.fromRGBO(240, 245, 241, opacity);
  static Color red(double opacity) => Color.fromRGBO(184, 14, 2, opacity);
  static Color blue(double opacity) => Color.fromRGBO(65, 156, 194, opacity);
  static Color grey(double opacity) => Color.fromRGBO(167, 168, 167, opacity);
}
